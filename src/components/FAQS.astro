<section class="faqs">
    <h2>FAQS</h2>

    <div>
        <details class="green">
            <summary>¿Cuánto tiempo tarda en estar lista mi web?</summary>
            <p>Su página web estará lista en un plazo de entre 1 y 5 dias hábiles.</p>
        </details>
    
        <details class="yellow">
            <summary>¿Puedo modificar mi web después de que esté lista?</summary>
            <p>Una vez terminada su web, puede modificar la información, fotos y textos de la web sin ningún coste extra.</p>
        </details>

        <details class="red">
            <summary>¿Tengo que pagar por el alojamiento de la web?</summary>
            <p>No tendrás que pagar por el alojamiento de la web ni por el dominio. Además nosotros nos encargamos en la contratación de estos servicios para que no tengas que preocuparte por ello.</p>
        </details>  
    </div>
</section>

<style>
    .faqs {
        width: 100%;
        margin-top: 70px;
        padding: 30px;
        background-color: var(--color-white);
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    .faqs div {
        width: 800px;
        max-width: 100%;
        margin: auto;
        display: flex;
        flex-direction: column;
        gap: 5px;
        border-radius: 10px;
    }

    .green {
        background-color: var(--color-green);
    }

    .yellow {
        background-color: var(--color-yellow);
    }

    .red {
        background-color: var(--color-red);
    }

    .faqs h2 {
        text-align: center;
        margin-bottom: 20px;
        font-size: 2rem;
    }

    details {
        margin-bottom: 15px;
        padding: 10px;
        color: #fff;
    }

    summary {
        font-size: 1.1rem;
        font-weight: bold;
        cursor: pointer;
        color: var(--color-dark);
        padding: 5px 10px;
        transition: background-color 0.3s;
    }

    p {
        padding: 10px;
        font-size: 1rem;
        color: var(--color-dark);
        background-color: var(--color-light-grey);
        border-radius: 5px;
        margin-top: 10px;
        border-top: 1px solid var(--color-white);
    }
</style>
